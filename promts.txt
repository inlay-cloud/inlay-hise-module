This is a HISE project (not real js code). 
Implement myFunc in AdditionalSourceCode (C++), to be invoked in Scripts/LicenseManager/LicenseManager.js
The func should return true/false depending on equality of the passed params to the func
See HiseSDK dir to reference current HISE sdk and tutorials, pay additional attention to HiseSDK/hise_tutorial/ExternalFloatingTileTest - this is official tutorail on how to add custom c++. You may change the way how the func is called from scripts, the point is that I need the way how to communicate betwenn scripts and c++ code. Hise SDK cannot be modified, the only chnages that are alloed are 1) adding files to AdditionalSourceCode 2) change in Scripts/LicenseManager/LicenseManager.js
It seems that it's not possible to extend Hise scripting by the myFunc. Hovever, you may probavly make script to c++ communication thought UI component (like ExternalTestComponent does in the tutorial)

Fix error: LicenseManager.js (41): Unknown function 'fromBase64Encoding'
Take in account:
- This is a HISE dsl project (not real js code). 
- See HiseSDK dir to reference current HISE sdk and tutorials.

Findout implementation details of String.hash()
Take in account:
- This is a HISE dsl project (not real js code). 
- See HiseSDK dir to reference current HISE sdk and tutorials.

Implement decodeFromHex in LicenseManager.js. The Func should accept hex string and return decoded string
Take in account:
- This is a HISE dsl project (not real js code). 
- See HiseSDK dir to reference current HISE sdk and tutorials.


	1.	Crawl all nested HISE scripting documentation pages from here: https://docs.hise.dev/scripting/index.html
	2.	Extract text + code snippets. (omit images and links)
	3.	Build a structured Markdown file with headings reflecting the site hierarchy.
	4.	Package it as a single file: hise_scripting_docs.md.
